[pytest]
testpaths = tests
addopts =
    --html=reports/report.html
    --self-contained-html
    --json-report
    --json-report-file=reports/report.json
    -v
    --tb=short
base_url = http://localhost:3000
