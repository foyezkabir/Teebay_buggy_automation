import styled from 'styled-components'

const StyledFilterFormBorder = styled.div`
  display: block;
  width: 80%;
  padding-left: 6em;
`

const StyledButtonsHolder = styled.div`
  margin-top: 30px;
  display: flex;
  justify-content: center;
`

const StyledHeaderHolder = styled.div`
  display: flex;
  justify-content: center;
`

const StyledClearButtonHolder = styled.div`
  margin-right: 8px;
`;

const StyledCheckboxHolder = styled.div`
  margin-bottom: 10px;
`;

export {
  StyledFilterFormBorder, StyledButtonsHolder, StyledHeaderHolder, StyledClearButtonHolder,
  StyledCheckboxHolder,
};
